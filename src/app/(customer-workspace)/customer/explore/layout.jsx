// src/app/(customer-workspace)/customer/explore/layout.jsx

export default function ExploreLayout({ children }) {
    return (
      <div className="explore-layout">
        {/* Any layout elements for the explore section */}
        <main>{children}</main>
      </div>
    );
  }
